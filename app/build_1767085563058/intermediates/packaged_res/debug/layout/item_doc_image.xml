<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/photo_placeholder_bg"
    android:padding="4dp"
    android:clickable="true"
    android:focusable="true">

    <ImageView
        android:id="@+id/docs_image"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:scaleType="centerCrop"
        android:adjustViewBounds="true"
        android:visibility="invisible"
        android:contentDescription="Документ"
        android:clickable="true"
        android:focusable="true" />

    <ProgressBar
        android:id="@+id/docs_progress"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:indeterminateTint="@color/dark_blue"
        android:layout_gravity="center"
        android:visibility="gone"/>

    <!-- Индикатор для загрузки по требованию -->
    <ImageView
        android:id="@+id/download_indicator"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="center"
        android:src="@drawable/ic_download"
        android:background="@drawable/download_indicator_bg"
        android:clickable="true"
        android:focusable="true"
        android:contentDescription="Загрузить изображение"/>

</FrameLayout>